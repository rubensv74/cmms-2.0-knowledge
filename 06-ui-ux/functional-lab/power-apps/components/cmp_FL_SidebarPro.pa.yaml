# CMMS 2.0 FUNCTIONAL LAB — F01-00A — INSTANCE-SAFE SOURCE BASELINE
#
# Status: DIAGNOSTIC SAFE BASELINE
# Incident: FL-SC-001
# Target component: cmp_FL_SidebarPro
#
# IMPORTANT
# This canonical YAML intentionally contains NO CustomProperties.
# R5-TD proved that the hand-written Source Code declaration of Input/Text
# closed Studio when inserting an instance, while the same contract created
# manually in Studio passed R5-TM.
#
# Current Studio state may therefore contain an AppTitle property created
# manually through the Studio UI that is not represented here yet.
# Do not add CustomProperties to this YAML until R5-TS captures and validates
# the exact serialization generated by Studio.

ComponentDefinitions:
  cmp_FL_SidebarPro:
    DefinitionType: CanvasComponent

    Properties:
      Fill: =ColorValue("#07111F")
      Height: =700
      Width: =220

    Children:
      - conFLSB_Root:
          Control: GroupContainer@1.5.0
          Variant: ManualLayout
          Properties:
            BorderColor: =RGBA(0,0,0,0)
            BorderThickness: =0
            DropShadow: =DropShadow.None
            Fill: =ColorValue("#07111F")
            Height: =Parent.Height
            RadiusBottomLeft: =0
            RadiusBottomRight: =0
            RadiusTopLeft: =0
            RadiusTopRight: =0
            Width: =Parent.Width
            X: =0
            Y: =0
