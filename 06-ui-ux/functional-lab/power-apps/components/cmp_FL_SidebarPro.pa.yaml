# CMMS 2.0 FUNCTIONAL LAB — F01-00A-R1 — SIDEBAR ROOT-ONLY REPRODUCER
#
# Status: CORRECTIVE DIAGNOSTIC — PENDING POWER APPS STUDIO VALIDATION
# Incident: FL-SC-001
# Operation: REPLACE COMPLETE COMPONENT SOURCE
# Target component: cmp_FL_SidebarPro
#
# PURPOSE
# Determine whether a minimal CanvasComponent created from Source Code can be
# instantiated safely in the CMMS 2.0 Functional Lab app.
#
# IMPORTANT
# Replace the COMPLETE current cmp_FL_SidebarPro source with this content,
# beginning at `ComponentDefinitions:`.
# Do not keep any CustomProperties or children from the previous version.
#
# INTENTIONALLY REMOVED FOR THIS TEST
# - CustomProperties
# - Table inputs
# - Output properties
# - Event properties
# - Gallery
# - ModernText / Label / Button / Icon
# - navigation logic
# - conditional geometry
# - AutoLayout nesting
#
# VALIDATION
# 1. Source definition is accepted.
# 2. Save succeeds.
# 3. Insert ONE instance of cmp_FL_SidebarPro on an otherwise empty screen.
# 4. Studio must remain open and responsive.
# 5. The instance renders as a dark 220x700 surface.
# 6. Save again.
# 7. App Checker shows no new error attributable to the component.
#
# INTERPRETATION
# PASS -> minimal CanvasComponent infrastructure is instance-safe; restore the
#         sidebar incrementally until the first failing responsibility appears.
# FAIL -> investigate the CanvasComponent Source Code/baseline mechanism itself;
#         do not restore component complexity.

ComponentDefinitions:
  cmp_FL_SidebarPro:
    DefinitionType: CanvasComponent

    Properties:
      Fill: =ColorValue("#07111F")
      Height: =700
      Width: =220

    Children:
      - conFLSB_Root:
          Control: GroupContainer@1.5.0
          Variant: ManualLayout
          Properties:
            BorderColor: =RGBA(0,0,0,0)
            BorderThickness: =0
            DropShadow: =DropShadow.None
            Fill: =ColorValue("#07111F")
            Height: =Parent.Height
            RadiusBottomLeft: =0
            RadiusBottomRight: =0
            RadiusTopLeft: =0
            RadiusTopRight: =0
            Width: =Parent.Width
            X: =0
            Y: =0
