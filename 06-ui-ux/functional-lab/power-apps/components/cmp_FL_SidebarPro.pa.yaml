# CMMS 2.0 FUNCTIONAL LAB — F01-00A-R5-TD — TEXT INPUT DECLARATION ONLY
#
# Status: CORRECTIVE DIAGNOSTIC — PENDING POWER APPS STUDIO VALIDATION
# Incident: FL-SC-001
# Operation: REPLACE COMPLETE COMPONENT SOURCE
# Target component: cmp_FL_SidebarPro
#
# Previous results:
# - R1 root-only = PASS
# - R2 identity/text = PASS
# - R3 static containers = PASS
# - R4 static navigation = PASS
# - R5 Text+Boolean+Color = FAIL
# - R5-T Text declared+consumed = FAIL
#
# PURPOSE
# Separate declaration from consumption for Input/Text.
# This variant returns to the minimal R1-safe baseline and adds exactly one
# CustomProperty Input/Text. No child control references the property.
#
# INTERPRETATION
# PASS -> declaration alone is instance-safe; investigate consumption/binding next.
# FAIL -> declaration of Input/Text alone is sufficient to reproduce the crash
#         in this Source Code baseline; investigate schema/enhanced-property path.

ComponentDefinitions:
  cmp_FL_SidebarPro:
    DefinitionType: CanvasComponent

    CustomProperties:
      AppTitle:
        PropertyKind: Input
        DataType: Text
        Default: ="CMMS 2.0"

    Properties:
      Fill: =ColorValue("#07111F")
      Height: =700
      Width: =220

    Children:
      - conFLSB_Root:
          Control: GroupContainer@1.5.0
          Variant: ManualLayout
          Properties:
            BorderColor: =RGBA(0,0,0,0)
            BorderThickness: =0
            DropShadow: =DropShadow.None
            Fill: =ColorValue("#07111F")
            Height: =Parent.Height
            RadiusBottomLeft: =0
            RadiusBottomRight: =0
            RadiusTopLeft: =0
            RadiusTopRight: =0
            Width: =Parent.Width
            X: =0
            Y: =0
