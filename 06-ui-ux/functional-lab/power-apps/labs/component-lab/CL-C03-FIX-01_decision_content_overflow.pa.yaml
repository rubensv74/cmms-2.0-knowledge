# BLOCK CL-C03-FIX-01 — DecisionPanelPro content overflow repair
# TYPE: FIX — repair of failed visual QA increment
# ACTION: PATCH EXACT PROPERTIES IN EXISTING COMPONENT + HARNESS INSTANCE
# TARGET: cmp_FL_DecisionPanelPro + scr_ComponentLab / CL-C03 instance only
# DEPENDS ON: CL-C03 Studio visual evidence 2026-08-12
# TOUCHES: component Height, system/human card Height, CL-C03 instance Height, two harness legend Y positions
# DO NOT MODIFY: public contract, events, colors, typography, ComponentLab slot geometry, other components, functional screens
# DEFECT: RecommendationExplanation clips at the lower edge of conFLDecisionSystem with representative P-101 content.
# VALIDATION: full recommendation/explanation visible; human reason/authority visible; buttons clear of cards; no clipping; no black surfaces
# EXPECTED STATUS AFTER PASS: CL-C03 DECISIONPANEL PASS / READY_FOR_INTEGRATION
#
# APPLY IN STUDIO TO THE EXISTING cmp_FL_DecisionPanelPro IDENTITY.
# Do not add a second component copy.

# -----------------------------------------------------------------------------
# A — cmp_FL_DecisionPanelPro
# -----------------------------------------------------------------------------
# In ComponentDefinitions.cmp_FL_DecisionPanelPro.Properties replace only Height.
Height: =252

# In conFLDecisionSystem.Properties replace only Height.
Height: =104

# In conFLDecisionHuman.Properties replace only Height.
Height: =104

# -----------------------------------------------------------------------------
# B — scr_ComponentLab / current CL-C03 harness
# -----------------------------------------------------------------------------
# In cmpCLDecision.Properties replace only Height.
Height: =252

# In lblCLDecisionExpectedTitle.Properties replace only Y.
Y: =370

# In lblCLDecisionExpected.Properties replace only Y.
Y: =400

# No other property is part of this FIX.
